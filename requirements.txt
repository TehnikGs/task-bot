aiogram>=3.7,<4
httpx>=0.27
python-dotenv>=1.0

# Нужно только для SOCKS5-прокси (TELEGRAM_PROXY=socks5://...):
aiohttp-socks>=0.8
httpx[socks]>=0.27

# Нужно только если STT_BACKEND=local (распознавание речи на самом сервере):
# faster-whisper>=1.0
